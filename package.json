{
  "name": "virtual-life-browser-checks",
  "private": true,
  "type": "module",
  "scripts": {
    "prototype:material": "node web/material-prototype/server.mjs",
    "check": "node scripts/check-js.mjs",
    "test:e2e": "cargo build --locked --features web --bin web && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "node-pty": "1.1.0"
  }
}
