{
  "name": "virtual-life-browser-checks",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check web/app.js && node --check web/display.js && node --check playwright.config.js && node --check tests/browser/fixtures.js && node --check tests/browser/viewer.spec.js && node --test tests/display.test.js",
    "test:e2e": "cargo build --locked --features web --bin web && playwright test"
  },
  "devDependencies": { "@playwright/test": "1.63.0" }
}
